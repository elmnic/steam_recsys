from setuptools import find_packages, setup

setup(
    name='src',
    packages=find_packages(),
    version='0.1.0',
    description='Steam recommender system based on unique play time per user',
    author='HIS',
    license='MIT',
)
